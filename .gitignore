node_modules/
*.log
.DS_Store
.env
.env.local

# Keep source maps OUT of the published bundle — they double the size and
# the addon doesn't need them at runtime. Generated by `npm run build`.
rehau-bridge/dist/*.map
rehau-bridge/web/assets/*.map
